<?php
/**
 * Default layout for MyProject
 *
 * @category   MyProject_Layouts
 * @package    MyProject
 * @copyright  Copyright (c) 2008 MyProject
 * @author     Harold Thétiot (hthetiot)
 */

    // add blueprint stylesheet
    $this->headLink()->appendStylesheet('/css/lib/blueprint/screen.css', 'screen, projection');
    $this->headLink()->appendStylesheet('/css/lib/blueprint/print.css', 'print');
    $this->headLink()->appendStylesheet('/css/lib/blueprint/ie.css', 'screen, projection', 'IE');

    // add blueprint plugins stylesheet
    $this->headLink()->appendStylesheet('/css/lib/blueprint/plugins/fancy-type/screen.css', 'screen, projection');

    // @todo remove
    $this->headLink()->appendStylesheet('/css/layouts/default/layout.css', 'screen, projection');

    // set default meta
    $this->headMeta()->appendHttpEquiv('Content-Type', 'text/html; charset=utf-8');

    // set default title if empty
    $title = $this->headTitle()->getValue();
    if (empty($title)) {
        $this->headTitle()->set(__('Welcome to MyProject!'));

	    // set prepend title
    	$this->headTitle()->prepend(__('MyProject') . ' - ');
    }

      // set default meta
    $this->headMeta()->appendHttpEquiv('Content-Type', 'text/html; charset=utf-8');


    // display DocType
    echo $this->doctype() . "\n";
?>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en">
<head>
<?php echo $this->headTitle(); ?>
<?php echo $this->headMeta(); ?>
<?php echo $this->headLink(); ?>
<?php echo $this->headScript(); ?>
<?php echo $this->headStyle(); ?>

</head>
<body>
    <div class="container">

        <div id="header">

            <div class="span-5">
                <h1><?php echo $this->escape(__('MyProject')); ?><h1>
            </div>

            <div class="clear"></div>

        </div>

        <div id="content">
            <?php echo $this->placeholder('Zend_Layout')->content; ?>
        </div>

        <div id="footer">
        </div>
    </div
</body>
</html>
